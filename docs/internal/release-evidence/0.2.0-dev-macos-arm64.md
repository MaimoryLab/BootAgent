# BootAgent 0.2.0-dev macOS arm64 历史发行证据

> 历史记录：该包来自迁移前的预览流程，不代表当前发行实现。当前包由 `cmd/bootagent-release` 生成；本文件仅保留旧 SHA-256 以便审计。

> 补注（2026-08-04）：本文提到的 `cmd/bootagent-release`、`cmd/bootagent-rc`、
> `cmd/bootagent-provider-smoke` 已于 `23805b0` 移除，职责交给
> `.github/workflows/build-artifacts.yml`。相关命令是历史背景，不可执行。

状态：`technical-preview-unsigned`，不是 Stable。

- 构建平台：macOS arm64
- 产物：`BootAgent-0.2.0-dev-technical-preview-unsigned-macos-arm64.zip`
- SHA-256：`dad3f145b8710caf45c7de1cc8e8dd6fdfbdb00f6b6dc5cedf06dc49348cd46e`
- Manifest 构建时间：2026-07-26T10:14:28.949648Z
- 原生 cleanroom 复核日期：2026-07-28
- cleanroom 结果：通过

复核使用仓库内的 `tests/macos_cleanroom_test.sh`，将上述 ZIP 解压后的原生 `BootAgent/BootAgent` 作为 `ONEAGENT_PACKAGED_BINARY`。检查覆盖隔离 HOME、权限、备份、密钥不进入 profile/日志、本地 GUI 安全头和真实用户配置目录不被修改。

这份证据只对应上面列出的精确 SHA-256。任何包体变化都必须重新生成 manifest、重新运行 cleanroom，并更新证据；它不构成签名、公证或 Stable 认证。
